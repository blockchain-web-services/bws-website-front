name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          # anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # Optional: Customize the trigger phrase (default: @claude)
          # trigger_phrase: "/claude"

          # Optional: Trigger when specific user is assigned to an issue
          # assignee_trigger: "claude-bot"

          # Configure Claude's behavior for auto-merge
          claude_args: |
            --max-turns 15
            --allowedTools "Bash(grep:*),Bash(find:*),Bash(ls:*),Bash(cat:*),Bash(git:*),Edit,MultiEdit,Read,Write,Glob,Grep"
            --system-prompt "You are fixing a test that ALREADY FAILED. DO NOT RE-RUN TESTS!

              CRITICAL RULES:
              - The test already ran in CI and failed
              - You have the error details in the issue
              - DO NOT install dependencies or Playwright
              - DO NOT run npm install, build, or test commands
              - ONLY read files and fix the issues

              STEPS TO FOLLOW:
              1. Read the error/violations in the issue
              2. Find the relevant source files (usually in src/)
              3. Fix the specific issue:
                 - For 'link-name': Add aria-label to links
                 - For 'image-alt': Add alt text to images
                 - For 'color-contrast': Adjust CSS colors
              4. Use Edit/MultiEdit to fix files
                 - If MultiEdit fails with 'Found X matches', use replace_all: true
                 - Or provide more context to make the string unique
              5. Commit with: 'Fixed: [test name]' and 'Claude-Fix-Attempt'
              6. Merge directly to main/master branch
              7. CRITICAL: ALWAYS update the issue before finishing:
                 - If successful: Comment what was fixed, then close issue
                 - If failed: Comment what error occurred and what needs manual intervention
                 - NEVER leave the issue without a final status comment
              8. A new issue will be created if there are more failures

              ERROR HANDLING:
              - If you encounter ANY error (edit fails, git fails, etc), you MUST:
                1. Comment on the issue explaining the error
                2. Suggest what manual intervention is needed
                3. Still attempt to close the issue or mark it as blocked

              Remember: The test ALREADY ran. Just fix the code!
              ALWAYS report your final status to the issue (success or failure)!"

          # Optional: Advanced settings configuration
          # settings: |
          #   {
          #     "env": {
          #       "NODE_ENV": "test"
          #     }
          #   }
